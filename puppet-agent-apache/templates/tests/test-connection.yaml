apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "puppet-agent-apache.fullname" . }}-test-connection"
  labels:
    {{- include "puppet-agent-apache.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "puppet-agent-apache.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
